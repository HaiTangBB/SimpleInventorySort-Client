package name.modid;

import net.fabricmc.api.ModInitializer;

public class SortInventoryMain implements ModInitializer {
    @Override
    public void onInitialize() {
        // Main source set: Only mod metadata registration, no server content.
        // This method is left intentionally empty for a pure client mod structure
        // where common initialization is not required.
    }
}
